# HabitHive Frontend environment variables
# Copy to .env and fill values as needed.

# Base URL of the HabitHive backend API (FastAPI)
# Example (local): http://localhost:3001
REACT_APP_API_BASE_URL=
